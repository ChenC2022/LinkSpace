{
    "name": "link-space",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "pages:dev": "wrangler pages dev dist --kv=LINKS",
        "deploy": "npm run build && wrangler pages deploy dist"
    },
    "dependencies": {
        "vue": "^3.4.0",
        "vue-router": "^4.2.5",
        "qrcode": "^1.5.3"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^5.0.0",
        "vite": "^5.0.0",
        "wrangler": "^3.22.0"
    }
}